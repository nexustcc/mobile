package com.example.nexustcc;

import androidx.appcompat.app.AppCompatActivity;
import androidx.recyclerview.widget.RecyclerView;

import android.content.ClipData;
import android.os.Bundle;

import java.util.List;

public class ContainerGrupos extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_container_grupos);

//        RecyclerView recyclerView = findViewById(R.id.recyclerview);
//        List<Item> item = SQLhelper.getInstance(this).listBook();
//
//        recyclerView.setAdapter(new ListagemGrupos (item));
    }
}