<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nexustcc">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NEXUSTCC"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".Teste"
            android:exported="false" />
        <activity
            android:name=".ListagemItens"
            android:exported="false" />
        <activity
            android:name=".ItemContainerGruposActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".ListagemGrupo" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".ContainerGrupos" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".Formulario" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".VisaoGeral" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".Integrantes" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".ListagemGruposAntiga" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".SplashScreenActivity" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".Login"
            android:exported="false" />
         <activity
         android:name=".SplashScreenActivity"
         android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>

</manifest>